ADD_SUBDIRECTORY (rules)

INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR})

ADD_CXXTEST(MOSESPLNSynergyUTest)
ADD_CXXTEST(PredictiveImplicationScopeUTest)

TARGET_LINK_LIBRARIES(MOSESPLNSynergyUTest
  ${ATOMSPACE_LIBRARIES}
  ${COGUTIL_LIBRARY}
)

TARGET_LINK_LIBRARIES(PredictiveImplicationScopeUTest
  pln-types
  ${ATOMSPACE_LIBRARIES}
  ${COGUTIL_LIBRARY}
)
